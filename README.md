# Django E-Commerce App

A basic e-commerce web application built with Django — product listings, a shopping flow, and order handling backed by a SQLite database.

## Stack

Django, SQLite, media/static file handling for product images.

## Note

This was an early Django learning project, built to practice models, views, and templates in a real-world app shape.
